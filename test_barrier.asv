%% Exercice 9.5
f = @(x) (x(1)-5)^2 + (x(2) - 3)^2;

g_k = {
    @(x) x(1) + x(2) - 3
    @(x) -x(1) + 2*x(2) - 4
};
epsilon = 1;
x = [0;0];

for k = 1:5
    q = penalty(f, g_k, h_k, mu);
    [x, No_of_iterations] = nonlinearmin(q, x, "DFP", 1.0e-6, 0)
    h_k{1}(x)
    
    mu = mu * 5;
end